package br.com.felipeacerbi.githubapi.base.view.adapter

import android.view.View

class LoadingViewHolder<T>(view: View) : BaseViewHolder<T>(view) {

    override fun bind(item: T) {
        // Not needed
    }
}